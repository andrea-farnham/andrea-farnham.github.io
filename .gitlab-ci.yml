image: rocker/verse:4.2

pages:
  stage: deploy
  before_script:
  - R -e "install.packages('quarto')"
  
  script: 
  - cd Docs
  - R -e "quarto::quarto_render()"        

  artifacts:    
    paths:
    - public
    - Docs/_site
    
    
  only:
  - master
  interruptible: true

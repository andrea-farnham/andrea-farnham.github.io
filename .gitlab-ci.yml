image: rocker/verse:4.2

pages:
  stage: deploy
  before_script:
  - R -e "install.packages('quarto')"
  
  script: 
  - cd Docs
  - R -e "quarto::quarto_render()"       
  - cp -r public .public 

  artifacts:    
    paths:
    - public
    
    
  only:
  - master
  interruptible: true

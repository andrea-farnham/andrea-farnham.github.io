image: registry.gitlab.com/quarto-forge/docker/quarto


pages:
  stage: deploy
  before_script: 
    
  script:
  - R -e "quarto::render_site('Docs')"
  - cp Docs/_site/* public


        

  artifacts:    
    paths:
    - public
    
    
  only:
  - master
  interruptible: true

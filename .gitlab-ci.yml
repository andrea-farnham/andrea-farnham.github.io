version: "3.9"
services:
  rstudio:
    image: registry.gitlab.com/quarto-forge/docker/rstudio
    ports:
      - "8787:8787"

pages:
  stage: deploy
  before_script: 
    
  script:
  - R -e "quarto::render_site('Docs')"
  - cp Docs/_site/* public


        

  artifacts:    
    paths:
    - public
    
    
  only:
  - master
  interruptible: true

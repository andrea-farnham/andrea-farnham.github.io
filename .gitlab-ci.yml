image: rocker/verse:4.0.0

pages:
  stage: deploy
  before_script:
  - cp style.css public/  
  
  script:
  - R -e "rmarkdown::render_site()"
        

  artifacts:    
    paths:
    - public    
    
    
  only:
  - master
  interruptible: true
